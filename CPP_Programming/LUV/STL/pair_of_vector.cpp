#include<bits/stdc++.h>

using namespace std;

int main(){
    vector < pair<int,int>> v;
    int a,b;
    cin>>a>>b;
    v.push_back({a,b});
    cout<<v[0].first<<" "<<v[0].second<<endl;
    return 0;
}